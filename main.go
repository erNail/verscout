// Package main is the entry point for the verscout application,
// which handles the initialization and execution of the command line interface.
package main

import (
	"github.com/erNail/verscout/cmd"
)

func main() {
	cmd.Execute()
}
